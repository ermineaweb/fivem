version: "3"

services:
  fivem:
    build:
      context: ./
      dockerfile: ./dockerfile
    stdin_open: true
    tty: true
    volumes:
      - ./server.cfg:/srv/server.cfg
      - ./cache:/srv/cache
    ports:
      - "30120:30120/tcp"
      - "30120:30120/udp"
    depends_on:
      - mariadb

  db:
    image: mariadb
    environment:
      - MARIADB_ROOT_PASSWORD=fivemRoRo124578
      - MARIADB_DATABASE=es_extended

  adminer:
    image: adminer
    ports:
      - 8080:8080
    depends_on:
      - fivem
